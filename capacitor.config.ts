import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.primaya.ambulance',
  appName: 'Primaya Ambulans IGD',
  webDir: 'dist'
};

export default config;
